# Shpigotskiy Art Space — Сайт

Маркетинговый сайт студии творческого развития. Чистый HTML/CSS/JS — сборка не требуется.

---

## Структура проекта

```
bot1/
├── index.html              # Telegram Mini App (не трогать)
├── media/                  # Медиафайлы (фото)
│   ├── acting/
│   ├── guitar/
│   ├── teachers/
│   └── shpigotskiy.jpg
└── website/                # Сайт (эта папка)
    ├── index.html          # Главная страница
    ├── teachers.html
    ├── events.html
    ├── courses.html
    ├── contacts.html
    ├── offer.html
    ├── miniapp.html        # Telegram Mini App — лаунчер (v0.6)
    ├── directions/
    │   ├── index.html      # Все направления
    │   ├── guitar.html
    │   ├── vocals.html
    │   ├── painting.html
    │   ├── acting.html
    │   └── dance.html
    ├── account/            # Личный кабинет + админ-панель
    │   ├── login.html      # Вход
    │   ├── register.html   # Регистрация
    │   ├── recover.html    # Восстановление доступа
    │   ├── dashboard.html  # Главная кабинета (+ уведомления)
    │   ├── courses.html    # Мои курсы
    │   ├── lesson.html              # Просмотр урока (v0.4)
    │   ├── schedule.html            # Единый календарь: занятия + события + дедлайны (v0.6)
    │   ├── attendance.html          # Посещаемость (v0.5)
    │   ├── homework.html            # Домашние задания (v0.5)
    │   ├── achievements.html        # Профиль достижений (v0.6)
    │   ├── certificates.html        # Сертификаты (v0.5)
    │   ├── portfolio.html           # Цифровое портфолио (v0.6)
    │   ├── progress.html            # Профиль развития (v0.5)
    │   ├── notifications.html       # Центр уведомлений (v0.6)
    │   ├── settings.html            # Настройки аккаунта (v0.6)
    │   ├── parent.html              # Кабинет родителя (v0.5)
    │   ├── subscriptions.html       # Мои абонементы (v0.3)
    │   ├── payments.html            # История платежей + транзакции (v0.3/v0.6)
    │   ├── shop.html                # Оплата и покупки (v0.3)
    │   ├── admin.html               # Админ: ученики (v0.3)
    │   ├── admin-parents.html       # Админ: родители (v0.5)
    │   ├── admin-subscriptions.html # Админ: абонементы (v0.3)
    │   ├── admin-courses.html       # Админ: курсы + структура LMS (v0.3/v0.4)
    │   ├── admin-attendance.html    # Админ: посещаемость (v0.5)
    │   ├── admin-homework.html      # Админ: домашние задания (v0.5)
    │   ├── admin-certificates.html  # Админ: сертификаты (v0.5)
    │   ├── admin-achievements.html  # Админ: достижения (v0.5)
    │   ├── admin-events.html        # Админ: мероприятия (v0.6)
    │   ├── admin-portfolio.html     # Админ: портфолио (v0.6)
    │   └── admin-payments.html      # Админ: платежи (v0.3)
    ├── css/
    │   ├── style.css       # Дизайн-система публичного сайта
    │   └── account.css     # Стили кабинета и админки
    └── js/
        ├── main.js         # Скрипты публичного сайта
        ├── api.js          # Моковый API-слой
        ├── auth.js         # Защита маршрутов + роли + сессия
        └── account.js      # Логика страниц кабинета и админки (+ навигация, поиск, Telegram-адаптер)
```

---

## Зависимости

**Никаких** — браузер и любой локальный HTTP-сервер.

> Важно: открывать файлы через `file://` нельзя — шрифты Google Fonts и относительные пути к изображениям требуют HTTP-сервера.

---

## Запуск локально

Запускать сервер нужно из **корня репозитория** (`bot1/`), а не из папки `website/`. Иначе пути к изображениям (`../media/`) не разрешатся.

### Вариант 1 — Python (рекомендуется, есть на macOS/Linux)

```bash
cd /путь/к/bot1
python3 -m http.server 8000
```

Открыть в браузере: [http://localhost:8000/website/](http://localhost:8000/website/)

### Вариант 2 — Node.js (npx serve)

```bash
cd /путь/к/bot1
npx serve . -p 8000
```

Открыть: [http://localhost:8000/website/](http://localhost:8000/website/)

### Вариант 3 — VS Code Live Server

1. Установить расширение **Live Server** (Ritwick Dey).
2. Открыть папку `bot1/` в VS Code.
3. Правый клик на `website/index.html` → **Open with Live Server**.
4. Сервер запустится от корня репозитория автоматически.

### Вариант 4 — Node.js (http-server)

```bash
npm install -g http-server
cd /путь/к/bot1
http-server . -p 8000
```

---

## Страницы

| URL | Файл |
|-----|------|
| `/website/` | `website/index.html` |
| `/website/directions/` | `website/directions/index.html` |
| `/website/directions/guitar.html` | Гитара |
| `/website/directions/vocals.html` | Вокал |
| `/website/directions/painting.html` | Живопись |
| `/website/directions/acting.html` | Актёрское |
| `/website/directions/dance.html` | Танцы |
| `/website/teachers.html` | Преподаватели |
| `/website/events.html` | Мероприятия |
| `/website/courses.html` | Онлайн-курсы |
| `/website/contacts.html` | Контакты |
| `/website/offer.html` | Публичная оферта |
| `/website/account/login.html` | Вход в кабинет (v0.2) |
| `/website/account/dashboard.html` | Личный кабинет (v0.2) |
| `/website/account/schedule.html` | Расписание / календарь (v0.2) |
| `/website/account/courses.html` | Мои курсы (v0.2) |
| `/website/account/lesson.html` | Просмотр урока (v0.4) |
| `/website/account/attendance.html` | Посещаемость (v0.5) |
| `/website/account/homework.html` | Домашние задания (v0.5) |
| `/website/account/achievements.html` | Профиль достижений (v0.6) |
| `/website/account/certificates.html` | Сертификаты (v0.5) |
| `/website/account/portfolio.html` | Цифровое портфолио (v0.6) |
| `/website/account/progress.html` | Профиль развития (v0.5) |
| `/website/account/notifications.html` | Центр уведомлений (v0.6) |
| `/website/account/settings.html` | Настройки аккаунта (v0.6) |
| `/website/account/parent.html` | Кабинет родителя (v0.5) |
| `/website/account/subscriptions.html` | Мои абонементы (v0.3) |
| `/website/account/payments.html` | История платежей + транзакции (v0.3/v0.6) |
| `/website/account/shop.html` | Оплата и покупки (v0.3) |
| `/website/miniapp.html` | Telegram Mini App — лаунчер (v0.6) |
| `/website/account/admin.html` | Админ: ученики (v0.3) |
| `/website/account/admin-parents.html` | Админ: родители (v0.5) |
| `/website/account/admin-subscriptions.html` | Админ: абонементы (v0.3) |
| `/website/account/admin-courses.html` | Админ: курсы + структура LMS (v0.4) |
| `/website/account/admin-attendance.html` | Админ: посещаемость (v0.5) |
| `/website/account/admin-homework.html` | Админ: домашние задания (v0.5) |
| `/website/account/admin-certificates.html` | Админ: сертификаты (v0.5) |
| `/website/account/admin-achievements.html` | Админ: достижения (v0.5) |
| `/website/account/admin-events.html` | Админ: мероприятия (v0.6) |
| `/website/account/admin-portfolio.html` | Админ: портфолио (v0.6) |
| `/website/account/admin-payments.html` | Админ: платежи (v0.3) |

---

## Личный кабинет (v0.2)

Система авторизации и личный кабинет ученика. Реализованы:

- **Авторизация:** регистрация по телефону или email, вход, восстановление доступа, защищённые маршруты (редирект на вход для неавторизованных).
- **Кабинет ученика:** имя, направление, преподаватель, дата ближайшего занятия, расписание на неделю, остаток занятий, срок абонемента, статус оплаты.
- **Мои курсы:** список приобретённых онлайн-курсов с прогрессом в процентах и кнопкой перехода.
- **Расписание:** календарное отображение занятий по месяцам с детализацией дня.

### Демо-доступ

Без регистрации можно войти под демо-аккаунтом:

```
Логин:  demo@shpigotskiy.art
Пароль: demo1234
```

### Как это устроено (важно для интеграции)

> Реального бэкенда нет — используются **моковые данные и mock-API** на стороне браузера.

- `js/api.js` — единственная точка доступа к данным. Все страницы вызывают `window.API.*`, который сейчас возвращает Promise с тестовыми данными и хранит пользователей/сессию в `localStorage` (`sas_users`, `sas_session`). Когда появится настоящий backend — меняется **только этот файл** (методы заменяются на `fetch(...)`), код страниц трогать не нужно.
- `js/auth.js` — защита маршрутов и управление сессией. Подключается до `account.js`, выполняется немедленно: неавторизованных перенаправляет на `login.html`.
- `js/account.js` — логика всех страниц кабинета (один файл, секции выполняются только при наличии нужных элементов).
- `css/account.css` — стили кабинета, переиспользуют переменные из `style.css`.

---

## Абонементы, платежи и админ-панель (v0.3)

Управление абонементами, платежами и базовое администрирование. Реализованы:

- **Абонементы** (сущность `Абонемент`: название, количество занятий, стоимость, срок действия, статус `активный/завершён/заморожен`). В кабинете на главной показываются текущий абонемент, остаток занятий, дата окончания и статус.
- **Мои абонементы** — история всех абонементов ученика (название, дата покупки, дата окончания, количество занятий, статус).
- **Платежи** (сущность `Платёж`: дата, сумма, назначение, статус). Раздел «История платежей» в кабинете.
- **Покупка** — интерфейсы покупки абонемента, продления абонемента и покупки онлайн-курса (страница «Оплата и покупки»). Реальная оплата не подключена; все покупки проходят через единую функцию-заглушку `processCharge()` — точку будущей интеграции платёжных систем.
- **Уведомления** в кабинете (без email/SMS): осталось менее 3 занятий, абонемент заканчивается менее чем через 7 дней, есть неоплаченные услуги.
- **Админ-панель** (роль `admin`, защищённый раздел): ученики (список, поиск, карточка), абонементы (создание/редактирование/удаление), курсы (создание/редактирование/удаление), платежи (просмотр).

### Демо-доступ

```
Ученик    — demo@shpigotskiy.art  / demo1234
Админ     — admin@shpigotskiy.art / admin1234
```

Админ после входа попадает в админ-панель (`admin.html`); ученики — в свой кабинет.

### Что добавилось в архитектуре

- **Новые namespace в `API`:** `subscriptions`, `payments`, `notifications`, `admin`; расширены `courses` (каталог + покупка) и `student.profile` (данные берутся из активного абонемента).
- **Данные** хранятся в `localStorage`: `sas_subscriptions`, `sas_payments`, `sas_courses` (плюс прежние `sas_users`, `sas_session`). Сид-данные создаются один раз; апгрейд с v0.2 происходит автоматически.
- **Роли:** `auth.js` различает публичные, пользовательские и админ-страницы; ученик не может открыть админ-раздел.
- **Боковое меню** кабинета и админки рендерится из `account.js` (`[data-cab-sidebar]`) — навигация в одном месте, без дублирования по страницам.
- **Точка интеграции оплаты** — `processCharge(order)` в `api.js`: сейчас возвращает успешный мок, позже заменяется вызовом реального провайдера (Kaspi / CloudPayments / Stripe …).

---

## Онлайн-курсы / LMS (v0.4)

Полноценная система онлайн-обучения поверх каталога курсов. Реализованы:

- **Структура курса:** Курс → Модули → Уроки. Уроки четырёх типов: видео, текст, изображение, файл.
- **Просмотр урока** (`lesson.html`): дерево курса слева, контент справа, навигация «предыдущий/следующий», хлебные крошки. Доступ только к приобретённым курсам.
- **Прогресс:** отметка «Урок пройден», процент прохождения, количество пройденных/оставшихся уроков, дата последнего просмотра. Хранится отдельно (`sas_lms_progress`).
- **Мои курсы:** количество модулей и уроков, прогресс, дата последнего занятия, кнопка «Начать / Продолжить / Пройти заново» с переходом к нужному уроку.
- **Админ — структура курса:** в разделе «Курсы» кнопка «Структура» открывает редактор модулей и уроков (создание, редактирование, удаление, загрузка материалов).

**Namespace:** `API.lms` (`modules`, `lessons`, `progress`), `API.courses.courseDetail`.
**Данные:** `sas_lms_modules`, `sas_lms_lessons`, `sas_lms_progress`.

---

## Ученик · Родитель · Преподаватель (v0.5)

Система взаимодействия ученика, родителя и преподавателя. Роль преподавателя выполняет
владелец студии через **админ-панель** (роль `admin`). Реализованы:

- **Роль `parent` и кабинет родителя** (`parent.html`): родитель привязан к одному или нескольким ученикам; по каждому ребёнку — направление, преподаватель, ближайшее занятие, остаток занятий, статус оплаты, прогресс по онлайн-курсам, домашние задания, достижения, сертификаты и комментарии преподавателей.
- **Посещаемость** (`attendance.html`): статусы `присутствовал / уважительная / неуважительная / отсутствовал`, процент посещаемости, статистика за период и история. Управление — в админ-панели.
- **Домашние задания** (`homework.html`): задание (название, описание, дата выдачи, срок, материалы). Ученик загружает файл/изображение/видео и отправляет на проверку. Статусы: `назначено / отправлено / проверено / требуется доработка`.
- **Проверка ДЗ:** преподаватель (админ) просматривает ответы, оставляет комментарии и меняет статус (`admin-homework.html`).
- **Сертификаты** (`certificates.html`): название, дата, описание, оформление; просмотр и «скачивание» учеником и родителем.
- **Достижения** (`progress.html`, кабинет родителя): первый концерт, выступление, месяц без пропусков, завершён курс, участие в выставке/спектакле и т.д.
- **Комментарии преподавателя:** рекомендации, замечания, комментарии о прогрессе — добавляются из карточки ученика, видны ученику и родителю.
- **Профиль развития** (`progress.html`): сводка — посещаемость, завершённые курсы, сертификаты, достижения, комментарии преподавателей.
- **Админ-панель:** управление родителями, посещаемостью, домашними заданиями, сертификатами, достижениями; комментарии — из карточки ученика.

### Демо-доступ (обновлено)

```
Ученик    — demo@shpigotskiy.art   / demo1234
Родитель  — parent@shpigotskiy.art / parent1234   (дети: Алина и Максим)
Админ     — admin@shpigotskiy.art  / admin1234
```

После входа: ученик → `dashboard.html`, родитель → `parent.html`, админ → `admin.html`.

### Что добавилось в архитектуре

- **Новые namespace в `API`:** `parent`, `attendance`, `homework`, `certificates`, `achievements`, `comments` (комментарии преподавателя), `integrations` (заглушка внешних каналов).
- **Данные:** `sas_academics`, `sas_attendance`, `sas_homework`, `sas_certificates`, `sas_achievements`, `sas_teacher_notes`. Сид-данные создаются один раз; апгрейд с предыдущих версий автоматический (добавляются родитель и второй ученик).
- **Роли:** `auth.js` различает страницы ученика, родителя и админа; админ — суперпользователь. Родитель не видит страниц ученика, и наоборот.

### Архитектура на будущее (НЕ реализовано)

Подготовлено к расширению, но намеренно не реализовано:

- **Реальные платежи** — через `processCharge()` (см. выше).
- **Внешние уведомления** — Telegram Bot/Mini App, push, email, SMS: зарезервирован `API.integrations` и внутренняя точка `notify(audience, message)` (сейчас no-op, каналы выключены).
- **Тесты к урокам** — зарезервирован `API.tests`, метод возвращает понятную ошибку-заглушку.
- **Геймификация, рейтинги, магазин** — модель достижений и единый mock-API совместимы с дальнейшим расширением.

> Большинство пунктов выше доработано в v0.6 — см. ниже.

---

## Платформа и экосистема (v0.6)

Завершение базовой образовательной платформы и подготовка экосистемы школы.

### Навигация (все роли)

На всех страницах кабинета (ученик, родитель, преподаватель/админ) добавлена единая
панель навигации под шапкой: **«Назад»**, **«На главную»**, **«На сайт»**, **хлебные
крошки** и **глобальный поиск**. Выход из аккаунта — с **подтверждением** (модальное окно).
Панель рендерится из `account.js` (реестр `PAGE_META`), поэтому существующий дизайн страниц
не меняется.

### Центр уведомлений (`notifications.html`)

Единый центр уведомлений с типами: занятия, платежи, домашние задания, сертификаты,
достижения, мероприятия, комментарии, абонемент. Поддержаны состояния **прочитано /
непрочитано / архив**, отметка «прочитать всё», счётчик непрочитанных в боковом меню.
Все действия в системе (выдача ДЗ, проверка, комментарий, сертификат, достижение,
мероприятие) теперь создают запись через единую точку `notify()`.

### Единый календарь (`schedule.html`)

Календарь объединяет **занятия, концерты, спектакли, выставки, мастер-классы и дедлайны
домашних заданий** (`API.calendar.month`). Цветовая легенда и детализация дня по типам.

### Цифровое портфолио (`portfolio.html`)

Портфолио ученика: **фото, видео, аудио, документы, дипломы, сертификаты**. Ученик и
родитель просматривают; преподаватель (админ) добавляет материалы (`admin-portfolio.html`).

### Профиль достижений (`achievements.html`)

Отдельная страница: достижения, сертификаты, завершённые курсы, участие в мероприятиях и
статистика посещаемости.

### Настройки аккаунта (`settings.html`)

Изменение профиля, смена пароля, настройки уведомлений (по типам), управление привязанными
аккаунтами (Telegram).

### Глобальный поиск

Поиск по курсам, урокам, преподавателям, домашним заданиям и мероприятиям из строки в
панели навигации (`API.search.global`), с учётом роли и доступа.

### Telegram Bot и Mini App (архитектура, готовая к подключению)

- **Bot:** `API.integrations.telegram()` описывает бота и темы уведомлений (занятия, ДЗ,
  комментарии, окончание абонемента, новые курсы/мероприятия). Авторизация и привязка —
  `auth.telegramAuth / linkTelegram / unlinkTelegram`. Доставка идёт через `notify()` →
  `dispatchExternal()` (каналы выключены до подключения backend).
- **Mini App:** лаунчер `miniapp.html` (подключает Telegram WebApp SDK) и адаптер в
  `account.js` — при открытии внутри Telegram включается полноэкранный режим и системная
  кнопка «Назад». Корректно открываются кабинет, расписание, ДЗ, курсы, достижения,
  сертификаты. Вне Telegram адаптер — no-op.

### Универсальный платёжный слой

`PAYMENT_GATEWAYS` (mock + заготовки Kaspi / CloudPayments / Stripe) и `processCharge()`
маршрутизируют любой платёж через активный шлюз — **бизнес-логика не зависит от провайдера**.
Добавлены **статусы транзакций** (`pending → processing → succeeded | failed`), **обработка
ошибок** и **история транзакций** (`API.payments.transactions`, раздел на `payments.html`).
Подключить реальную систему = добавить объект-шлюз с методом `charge(order)`.

### Что добавилось в архитектуре (v0.6)

- **Новые namespace в `API`:** `calendar`, `events`, `portfolio`, `search`; расширены
  `notifications` (центр), `payments` (шлюзы, транзакции, `pay`), `auth` (профиль, пароль,
  настройки, Telegram), `integrations` (Telegram/Mini App).
- **Данные:** `sas_notifications`, `sas_events`, `sas_portfolio`, `sas_transactions`
  (плюс настройки уведомлений и привязка Telegram в объекте пользователя). Сид-данные
  создаются один раз; апгрейд с предыдущих версий автоматический.
- **Общие страницы для всех ролей:** `settings.html`, `notifications.html`,
  `portfolio.html`, `achievements.html` (доступ с учётом роли; для родителя/админа —
  выбор ученика через `?student=`).

### Зарезервировано на будущее (НЕ реализовано)

`API.gamification`, `API.wallet` (внутренняя валюта), `API.ratings`, `API.seasons`
(сезонные события), `API.tests` — namespace-заглушки, возвращающие понятное сообщение
«появится в следующей версии». Магазин (`shop.html`) уже работает; его расширение под
внутреннюю валюту — следующий шаг. Мобильное приложение покрывается Telegram Mini App
и адаптивной вёрсткой.

---

## Исправленные ошибки (v0.1.1)

| Файл | Проблема | Решение |
|------|----------|---------|
| `directions/*.html` | Пути к фото `../media/` указывали на несуществующую `website/media/` | Изменено на `../../media/` |
| `teachers.html` | Путь `media/shpigotskiy.jpg` указывал на `website/media/` | Изменено на `../media/shpigotskiy.jpg` |
| `courses.html` | Кнопки имели и `data-modal`, и `onclick="setCourse()"` — модальное окно открывалось дважды | Удалён атрибут `data-modal` |
