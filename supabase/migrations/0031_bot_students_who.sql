-- =====================================================================
-- Телеграм-бот: различаем, КТО занимается — ребёнок (записал родитель) или
-- сам ученик/взрослый (записался за себя, напр. группа 18+ или подросток).
-- ---------------------------------------------------------------------
-- Добавляем колонку who к bot_students. Существующие строки → 'child'
-- (обратная совместимость). Пишет/читает только воркер (service_role).
-- Run в Supabase → SQL Editor → Run. ПОСЛЕ 0019.
-- =====================================================================

alter table public.bot_students
  add column if not exists who text not null default 'child'
  check (who in ('child', 'self'));
